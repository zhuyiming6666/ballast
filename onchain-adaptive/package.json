{
  "name": "ballast-adaptive-prototype",
  "version": "1.0.0",
  "private": true,
  "description": "Transparent Pi(tau_e,theta) checkpoint and amount-escrow prototype",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "bench": "hardhat run scripts/bench_gas.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.1.3",
    "ethers": "^6.15.0",
    "hardhat": "^2.28.6"
  }
}
